local LocalPlayer = game:GetService("Players").LocalPlayer -- Declaring LocalPlayer

delta.Settings:AddInput("Fov Changer", "I MADE THIS MYSELF I SWEAR", function(text)
  local number = tonumber(text) -- Converts the text into number first
  
  workspace.CurrentCamera.FieldOfView = number -- Set the Fov as the number input
end)
